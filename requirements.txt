aiogram==3.27.0
aiohttp==3.13.5
aiosqlite==0.22.1
asyncpg==0.30.0
psycopg2-binary==2.9.10
python-dotenv==1.2.2
SQLAlchemy==2.0.49
solders==0.21.0
httpx==0.27.2
solana==0.34.3
telethon==1.37.0
fastapi==0.115.6
uvicorn==0.32.1
# fastmcp REMOVED from the deploy manifest (July 2026 incident) -- its
# "client" extra requires starlette>=1.0.1, conflicting with
# fastapi==0.115.6's starlette<0.42.0 pin. This is a real, unresolved
# version conflict, not something to patch under live-outage pressure.
# bot/web.py's MCP mount degrades gracefully without this package (see the
# try/except guard in main.py around the dashboard-server startup) -- the
# dashboard/MCP tools are simply unavailable until this is solved properly,
# with time to test, not during a service-down emergency. See
# mcp_server/README.md for the follow-up plan.
